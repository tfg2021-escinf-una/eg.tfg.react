export interface ICountry {
  capital: string,
  code: string,
  callingCode: string
  currencyCodes: string [],
  flagImageUri: string,
  name: string, 
  numRegions: number,
  wikiDataId: string
};
